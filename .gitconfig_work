[user]
	name = Sam Morrison
	email = sam.d.morrison@pwc.com
[core]
	editor = vim
	excludesfile = /Users/927019/.gitignore_global
	pager = diff-so-fancy | less --tabs=4 -RFX
[alias]
  #list aliases
  la = "!git config -l | grep alias | cut -c 7-"
  ls = log --oneline --graph
  dl = "!git ls -1"
  dlc = diff --cached HEAD^
  st = status -s
  cl = clone
  ci = commit
  co = checkout
  br = branch
  diff = diff --word-diff
  dc = diff --cached
	d = difftool
  cz = npx git-cz
  tagsince = log $(git describe --tags --abbrev=0)..HEAD --oneline
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path =
[mergetool "sourcetree"]
	cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[commit]
	template = /Users/927019/.stCommitMsg
[pull]
	rebase = true
