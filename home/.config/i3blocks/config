[memory]
command=free -h | awk '/^Mem/ { print "MEM:"$3"/"$2 }' | sed s/i//g
interval=30

[ip]
command=hostname -i | awk '{ print "IP:" $1 }'
interval=once

[time]
command=date +'%Y-%m-%d %X'
interval=5

